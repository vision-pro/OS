# المساهمة

نرحب بالتحسينات التي تحافظ على هوية رؤية وعلى سلامة البيانات. قبل البدء، أنشئ فرعاً واضح الاسم من الفرع الافتراضي، ولا تعدل أي ملف أسرار أو بيانات إنتاجية.

## مسار العمل

1. ثبّت الاعتمادات باستخدام `pnpm install --frozen-lockfile`.
2. شغّل المشروع محلياً باستخدام `pnpm dev`.
3. غطِّ المنطق الجديد باختبار Vitest عندما يغيّر السلوك أو البيانات.
4. شغّل `pnpm check` و`pnpm test:ci` و`pnpm build` قبل فتح طلب دمج.
5. اشرح أثر التغيير على العربية والإنجليزية والهاتف ولوحة الإدارة في وصف طلب الدمج.

لا تضع شهادات عملاء أو تقييمات أو بيانات حجز تجريبية ضمن الشفرة أو الاختبارات. لا ترفع وسائط كبيرة إلى المستودع؛ استخدم التخزين السحابي المخصص للموقع.
